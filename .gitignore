# Site-specific Apache config (contains real IPs)
sites-enabled/000-default.conf

# Site-specific camera IPs
multicamera/config/cameras.json

# Camera preset images (site-specific photos)
cameracontrol/images/
multicamera/thumbnails/

# User-specific sequence routines
cameracontrol/sequences.json

# User-specific preset definitions
cameracontrol/presets.json

# ONVIF credentials (site-specific secrets)
multicamera/config/onvif-credentials.php

# Captured still images
cameracontrol/captures/
